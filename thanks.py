import sys
import os
#import jack
import jedi
#import farmhash
import hashlib
import genericpath
try:
    sys.argv[1] = os.environ
    #jack.Client(sys.argv[1])
    jedi.settings(True)
    #hashlib.new(farmhash.fingerprint128)
    set.update(genericpath)
except:
    try:
        genericpath
    except:
        hashlib.new(genericpath)
        #farmhash
        genericpath